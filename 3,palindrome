# include <stdio.h>
int main ()
{
int n,len;
char a[20];
printf ("enter the number of strings\n");
scanf ("%d"&n);
for (int i =0;i<n;i++)
{
printf ("enter the length of %d string\n");
scanf ("%d",&len);
a[len];
printf ("enter the character\n");
for (int i =0;i<len;i++)
{
scanf ("%s",&a[i]);
}

